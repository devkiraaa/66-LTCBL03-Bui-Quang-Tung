#include <stdio.h>
#include <math.h>

      int main(){
            int x;
            scanf("%d", &x);
      int m1 = (int)pow(x, 2);
      int m2 = (int)pow(x, 3);
      int m3 = (int)pow(x, 4);
      printf("%d %d %d", m1, m2, m3);
      return 0;
      }
